    const knex = require("knex")({
        client: "postgresql",
        connection: {
            database: "optee5",
            user: "postgres",
            password: "postgres",
        }
    })

    module.exports = { knex }