    const knex = require("knex")({
        client: "postgresql",
        connection: {
            database: "my_optee",
            user: "postgres",
            password: "postgres",
        }
    })

    module.exports = { knex }